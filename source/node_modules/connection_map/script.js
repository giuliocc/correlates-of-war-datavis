//Width and height
var w = 1200;
var h = 800;

//Create SVG element
var svg = d3.select("body")
	.append("svg")
	.attr("width", w)
	.attr("height", h);

// cshapes.features = cshapes.features.filter(function(d) { return d.properties.COWCODE != -1; });

var geomap = new GeoMap({
	id: "myGeoMap",
	container: svg,
	x: 0,
	y: 0,
	width: w,
	height: h,
	dataset: shapes,
	connect_data: war_connect
});
